#include "main.h"

/**
 * print_times_table
 * @n : argument
 * Return: returns nothing
 */

void print_times_table(int n)
{
	if (n <= 0 && n >= 15)
	{

	}
	else if (n >= 0 && n <= 15)
	{
		printf("%d\n", n);
	}
}
